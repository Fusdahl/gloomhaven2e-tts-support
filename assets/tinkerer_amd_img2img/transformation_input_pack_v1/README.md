# Tinkerer AMD img2img work pack (v1)

This pack is organized as one transformation per folder.

## 00_initial_reuse_atlas
Easy wins already reusable:
- `+0` (GH1 Tinkerer)
- `+1 wound` (GH1 Tinkerer)
- `+1 immobilize` (GH1 Tinkerer)
- filler and back included
- initial 5x5 atlas built for quick visual check

## 01-04 task folders
Each folder is one clear task with:
- `TASK.txt`
- `BASE_*` input image(s)
- `REF_*` style/value reference image(s)
- `OUTPUT_FILENAME.txt`

Drop completed outputs in `99_outputs_drop_here`.
